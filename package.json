{
  "name": "maily-app",
  "version": "1.0.0",
  "description": "Maily email management application",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "types:generate": "cd backend && export $(cat .env | grep -v '^#' | xargs) && ~/.local/bin/supabase gen types typescript --project-id zmuyzdhbecwgpmtauksc > ../shared/types/database.types.ts"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/bun": "latest"
  }
}
